# Anthropic — for blue team backbone, Coach agent, LLM judge
ANTHROPIC_API_KEY=

# OpenAI — for variant blue team (multi-model bake-off)
OPENAI_API_KEY=

# UiPath Automation Cloud (filled after Labs access lands).
# Run `uip login` (after `npm i -g @uipath/cli`) to populate these automatically.
UIPATH_URL=
UIPATH_ACCESS_TOKEN=

# HeyGen — video avatars
HEYGEN_API_KEY=

# ElevenLabs — voice synthesis (fallback path)
ELEVENLABS_API_KEY=
